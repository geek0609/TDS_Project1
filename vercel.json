{
    "version": 2,
    "builds": [
        {
            "src": "virtual_ta.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/virtual_ta.py"
        }
    ],
    "env": {
        "PYTHONPATH": "/var/task"
    }
} 